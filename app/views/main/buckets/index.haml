.top-menu
  %a.btn.btn-sm.btn-empty{ :onclick=>"Popup.go.form(this, 'Add new bucket', '/api/buckets/create', 'name', '')" } Create new bucket
  %a.btn.btn-sm.btn-empty{ :href=>'/buckets/archive' } Archive

- for bucket in @buckets
  .bucket
    %h4= bucket.as_link
    

= paginate @buckets

