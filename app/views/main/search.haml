.nudge

%h3 Searching for: #{params[:q]}

%h6 Found #{@buckets.count.pluralize(:bucket)}, #{@links.count.pluralize(:link)} and #{@notes.count.pluralize(:note)}

.nudge

- for el in @all_links
  = render el