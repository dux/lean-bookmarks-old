- @base_path = @link.path(:admin)

= link_to 'Show', @link.path, :style=>'float:right;', :class=>'no-pjax btn btn-default btn-xs'

%h1= @link.name

%br
= bs_tabs [[@base_path,'Bookmark']]
%br

= yield