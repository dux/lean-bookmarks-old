- @base_path = @user.path(:admin)

= link_to 'Show', @user.path, :style=>'float:right;', :class=>'no-pjax btn btn-default btn-xs'

%h1= @user.name

%br
= bs_tabs [['/admin/users','&larr;'], [@base_path,'User']]
%br

= yield